import tkinter as tk

gui = tk.Tk(className='Indio NLP')
# initialize gui variable to set window size

gui.geometry('400x600')
# set the window size

text = tk.Entry(gui)
# initialize text widget



gui.mainloop()
# window will not stop until the user manually exits out
